{
  "name": "jarvis-ai-system",
  "version": "1.11.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/worker.test.mjs desktop/test/*.test.cjs"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
